# 빌드 컨텍스트를 작게 유지하고, 로컬 docker build 가 Coolify 와 같게 굴게 한다
# (Coolify 는 새로 clone 해서 빌드하므로 git-ignore 된 것은 애초에 없다).
.git
.gitignore
.claude
.DS_Store
**/.DS_Store

# 아이콘 SVG 원본. Dockerfile 이 서빙할 파일만 골라 넣지만, 컨텍스트에서도 빼둔다.
brand

README.md
LICENSE
